package generated;
import java.util.concurrent.ExecutionException;

import recaf.stream.Stream;
import recaf.stream.StreamExtension;

public class TestStream {

  static Void println(Object o) {
    System.out.println(o);
    return null;
  }
  
  private recaf StreamExtension<Integer> alg = new StreamExtension<Integer>();
   
  recaf Stream<Integer> asyncRange() {
    yield! 1;
    yield! 2;
    yield! 3;
    yield! 4;
    yield! 5;
  } 
  
  recaf void consumeStream() {
     awaitFor (Integer x: asyncRange()) {
       System.out.println(x);
     }
  }
  
  
  public static void main(String args[]) throws InterruptedException, ExecutionException {
	new TestStream().consumeStream();
  }

}
